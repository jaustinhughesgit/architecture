# Capability Catalog

This catalog prevents future work from assuming a capability is absent merely because it is unfamiliar. It is intentionally incomplete and must grow as the platform is inspected and discussed.

| Capability | Status | Current or intended ownership | Notes / next documentation work |
| --- | --- | --- | --- |
| Indexed local Journal records and temporal query algebra | Implemented and deployed to development | `onevar-platform` | A content-addressed browser-local Compute package carries a bounded subject vocabulary while every fact is an independent immutable `journal_record` indexed by exact owner/app/subject/concept IDs in the Context worker. The first Vehicle Logbook blueprint covers distance, fuel, maintenance, repairs, recurrence rules, canonical unit conversion, weekly totals, latest events, and next-due calculation without adding vehicle branches to the generic runtime. Equivalent rebuilds advance one app lineage and successful mutations emit deduplicated Sunburst attention. Known inputs require no model or server. Source tests prove 25,001 records without the earlier event-state ceiling. Development workflow [33472701692](https://github.com/jaustinhughesgit/onevar-platform/actions/runs/33472701692) deployed release `76573fc59f870fe0a1f111e8f2b74b1a413934cd`, passed the repository gate and 26 reset-gated browser scenarios, and an independent deployed Transport/widget/attention/reload proof passed in 9.8 seconds. See [Indexed Journal entities](indexed-journal-entities.md) and [decision 0122](../decisions/0122-journals-store-growing-facts-as-indexed-records.md). |
| Clean-room ordinary artifacts and governed PDF creation | Implemented and deployed to development | `onevar-platform` | Stable `file_` entities own immutable SHA-256-bound `fver_` versions. Exact metadata/grants/receipts use bounded DynamoDB partitions while a dedicated private S3 bucket handles presigned byte transfer. Owner-only sharing freezes unique public-profile resolution to one recipient ID; revocation removes the access pointer. Reviewed `onevar.artifact.text_to_pdf@1` accepts one exact read/use-authorized UTF-8 text version and creates a PDF plus an idempotent source/output receipt tied to the root interaction. Development workflow [33297907699](https://github.com/jaustinhughesgit/onevar-platform/actions/runs/33297907699) passed the full repository gate and 24 reset-gated deployed browser scenarios; an independent deployed two-user artifact run also passed. Protected files and executable packages stay separate. Public artifact marketplace pricing, broader formats, and protected-file server Compute remain incomplete. See [decision 0114](../decisions/0114-ordinary-artifacts-separate-bytes-authority-and-execution.md). |
| Production-credit provenance | Implemented candidate | `onevar-platform` | Cash-backed Checkout credit and non-cash promotional credit use separate available/reserved ledger accounts. Promotional value pays only first-party platform interactions; marketplace and paid-provider liabilities require cash-backed credit. Legacy positive aggregates migrate conservatively as promotional. One unambiguous Checkout-funded marketplace debit retains its exact Session; publisher withdrawal verifies that Session's charge and source-links the Connect transfer so pending charge timing does not block valid earnings. Ambiguous provenance falls back to the settled balance. Unavailable settled funds keep the exact request reserved and due-indexed for stage-level automatic retry. See [decisions 0102](../decisions/0102-credit-provenance-gates-publisher-cash.md), [0103](../decisions/0103-publisher-transfers-link-exact-checkout-charge.md), and [0108](../decisions/0108-publisher-withdrawals-retry-after-settlement.md). |
| Clean-room public and entity shell | Implemented and deployed foundation | `onevar-platform` + `onevar-operations` | The private repositories contain a self-contained migration of the public homepage, `/newentity1` primary-entity creation with an opaque host-only session, and an exact `/<entityId>` command runtime whose `ui on` effect projects the graphical shell without creating a second runtime. Unit, build, CDK synthesis, local Chromium acceptance, isolated development and production acceptance, and live `1var.com` acceptance pass. The deployed application uses private S3, CloudFront routing, a same-origin HTTP API, Node.js 24 Lambda, and on-demand DynamoDB. GitHub deploys through repository- and environment-bound AWS OIDC; `onevar-operations` retains exact application and apex rollback records. Ordinary ContextDB, voice, and compiler-bounded Compute are deployed through Phase 3; protected assets and marketplace behavior remain later clean-room vertical slices rather than inherited POC behavior. See [decision 0052](../decisions/0052-clean-room-platform-and-domain-cutover.md). |
| Clean-room verified input-to-response kernel | Implemented and deployed | `onevar-platform` | Release `5cb00df` provides strict contracts, a content-addressed core catalog, worker-owned IndexedDB ContextDB, caller-local Paths, vocabulary-neutral Essence operations, one versioned current-value relation with observation history, loss-safe idempotent canonical publication, compact DynamoDB deltas, explicit private/public ordinary read audience, exact named-person graph hydration, review/reset, offline execution, and a second non-car domain. Unit, contract, compact-persistence, production-build, bundle-budget, and two-clean-browser acceptance pass. The deployed suite deliberately drops one publication and proves exact retry, denied-before-share, dirty/clean/dirty refresh, revocation, and reset-after-run in both isolated development (3/3, 14.1 seconds) and production/`1var.com` (3/3, 13.0 seconds). Known same-user interactions use no model or API. Compute installation, Entity Use Binding, JPL, middleware, ArrayLogic, voice, and Convert begin in Phase 3. See [decision 0054](../decisions/0054-worker-owned-context-and-exact-hydration.md) and the [clean-room roadmap](clean-room-roadmap.md). |
| Clean-room exact Compute boundary | Implemented and deployed | `onevar-platform` | Release `d1b02dc0a28af71704465bd92091f54820dbef02` deploys strict build-plan, manifest, Invocation Frame, installation, versioned Entity Use Binding/Binding Essence, property-scoped current relation, JPL, result/effect, trace, durable sequential ArrayLogic, Position, package, authoring, release-governance, and ordinary-audio contracts as compiler-bounded, hash-pinned JPL. Durable Convert freezes a semantic answer/effect plan before a second contract proposal; trusted code owns IDs, executable syntax, the selected subject, dependency ownership, and transition endpoints. Lexical plus compact int8/LSH Position retrieves candidates, while browser and server independently verify one content-addressed S3 package before exact invocation. New DynamoDB rows store bounded manifests and routing metadata, not executable programs; a bounded migration reader extracts only the manifest from a legacy row. The browser creates its own Path, binds exact entity/relation IDs, and atomically applies each declared effect and workflow checkpoint. Unit/API tests and clean two-browser acceptance prove `dirty -> clean`, model-free rebinding, immutable package install, no equal-name cross-binding, repeat invocation with zero model/discovery calls, reload-safe two-step ArrayLogic, idempotent effect receipts, and owner revocation against cached installers. Development workflow 32775939532 passed live two-stage authoring, the complete build/discovery/binding/invocation/effect canary, ordinary voice, and the unfiltered `gpt-5.4-mini` promotion gate 50/50 with zero critical failures; receipts cover 102 responses, 98,233 input tokens, and 26,527 output tokens. Production workflow 32777037295 promoted the same commit and passed reset-gated browser and voice acceptance; health and smoke checks verified the full release. JavaScript adapter execution, active middleware transport, non-`none` compensation, protected references, package signing/review, and marketplace governance remain fail-closed for Phase 4. See [decisions 0055](../decisions/0055-compiler-owned-jpl-and-hybrid-compute-execution.md) through [0064](../decisions/0064-phase-3-releases-the-governed-non-protected-jpl-boundary.md). |
| One-response ordinary transition authoring | Implemented, deployed, and live-proven in development | `onevar-platform` | Release `648ccf8605c35c40c28cf9735ddb6bcd6f4e3437` makes one `gpt-5.4-mini` semantic response authoritative for supported ordinary current-property transitions. Trusted code derives the fingerprint-bound inputs, exact dependency/effect, invocation frames, outputs, response projection, and compiler input; no second response copies already-frozen facts. An explicit exact-invocation hard stop owns its single selected-subject placeholder, and unused model-invented make/model parameters are removed before fingerprinting. Exact relation IDs remain installation-owned authority. Development workflow [33008411751](https://github.com/jaustinhughesgit/onevar-platform/actions/runs/33008411751) passed verification, deployment, 20 reset-gated browser scenarios, an exact paid vehicle-wash authoring canary with one provider response, independent `dirty -> clean` proof, and a real governed schedule. See [decision 0078](../decisions/0078-one-semantic-response-compiles-ordinary-transition-contracts.md). |
| Clean-room four-lane input trust surface | Implemented and deployed | `onevar-platform` | Release `6d55c528f9e8b11edc6b282639d4c29e387bb6b9` establishes Protected Essence, ordinary Essence, ordinary Convert, and Protected Convert as independent purpose/trust lanes. Release `620f51b72ac525475317590ffa7a2875aee7dc90` restores the walkie-talkie UI and gesture without importing the proof-of-concept runtime: hold to speak, drag vertically to route or mark Convert `3 -> 2 -> 3`, release to dispatch, and drag left to cancel and reveal typing. Separate Build and Discard actions are gone. AudioWorklet remains authoritative; a transient segmented MediaRecorder supplies ordinary embedded-browser compatibility only when the worklet yields no samples. Protected entry immediately stops and clears it, and mixed-trust held capture still blocks every remote transcription request. Development workflows [32798581498](https://github.com/jaustinhughesgit/onevar-platform/actions/runs/32798581498) and [32802829791](https://github.com/jaustinhughesgit/onevar-platform/actions/runs/32802829791), plus production workflows [32799020746](https://github.com/jaustinhughesgit/onevar-platform/actions/runs/32799020746) and [32803142123](https://github.com/jaustinhughesgit/onevar-platform/actions/runs/32803142123), passed reset-gated browser and voice acceptance. Protected authoring remains blocked until Phase 4A. See [decisions 0065](../decisions/0065-four-lane-input-trust-surface.md) and [0066](../decisions/0066-walkie-talkie-input-gesture.md). |
| Browser message and voice interaction | Implemented continuous-capture foundation / partial convergence | `aws` | Message slides out left of the mic controls in the selected Essence/Convert/protected mode, cancels capture on entry, and conditionally shows sanitized recent exchanges. Middle-button capture starts provisionally at pointer-down while quick taps discard the press. One physical microphone feeds four logical AudioWorklet lanes; entering protected is immediate, while leaving protected uses a cancellable 100 ms privacy guard before cutting a contiguous sample-clock segment. The live card reserves ordered slots from that same clock, masks protected slots immediately without retaining their plaintext, and settles ordinary words by capture switch index rather than callback arrival order. Ordinary PCM reaches `/transcribe` only after release. Protected PCM reaches only the browser-local Whisper worker, fails closed without local inference, and is cleared after use; model/runtime downloads carry no input audio. Held segments assemble once on release. Convert authoring stays visible across worker Essence-history refreshes without entering semantic history, and one repaired replay replaces rather than duplicates its failed input card. Real-browser AudioWorklet and local-model initialization are verified. Exact ordinary words remain recognition-latency-bound; wider device coverage, prepackaged/self-hosted model artifacts, independently verifiable client delivery, and first-use performance remain incomplete. See [decisions 0035](../decisions/0035-segmented-input-and-protected-use-policy.md) and [0043](../decisions/0043-sample-clock-voice-routing-and-local-protected-asr.md). |
| Segmented Convert requirement authoring | Implemented creation foundation / partial Edit | `aws` + `compute` | Ordinary Convert audio now turns each Convert → Essence → Convert excursion into an ordered hard-stop segment, adds missing terminal punctuation plus one line break, and submits one v1 envelope on release. A Button 3-started press closes the recorder immediately upon entering Button 2 instead of depending on the ordinary channel-switch dwell, and the deferred audio message preserves the press-scoped authoring marker. The live input draft renders the period and newline at the boundary, and authoritative audio transcription retains rather than flattens it. Convert bypasses Essence mutation, lexical indexing, and authoring-question replay, while a distinct bounded authoring context gives discovery up to 20 non-protected recent inputs and their proven Essence rows without a full graph snapshot. Both direct typed Convert submission and Transcribe hard-stop submission route the v1 envelope through durable discovery/build; that lifecycle preserves the envelope and context across polling, materializes validated JPL through ArrayLogic/Shorthand, deterministically retains explicitly declared invocation phrases, and installs all manifest-derived browser Paths. Positioned capability reuse now passes the same deterministic transition, response-family, and invocation hard stops before selection; a similar but behaviorally incompatible entity enters bounded correction instead of being silently reused. Message, preview, composition, authoring-context exclusion, declared-invocation, authoring-history, direct-module durable routing, and incompatible-reuse contracts are tested; physical device gesture timing remains a release smoke test. Edit still has target-aware Convert interception but must adopt the same multi-segment composer. See [decisions 0038](../decisions/0038-convert-hard-stops-compose-requirements.md) and [0045](../decisions/0045-convert-discovery-receives-bounded-ordinary-authoring-context.md). |
| Browser-local input rollback | Implemented foundation | `aws` | Message Back restores a bounded, identity-scoped pre-input checkpoint across graph/ContextDB, history, local translations, word map, and learned Path library. Server Path reconciliation is asynchronous. External effects and protected-asset use are not reversible. See [decision 0014](../decisions/0014-browser-local-input-rollback.md). |
| Account/group bootstrap (`/newUser`) | Implemented reset-safe identity foundation / partial lifecycle | `aws` + `compute` | The explicit new-user surface rotates stale host/domain identities only on the exact bootstrap route, creates one matching account/workspace pair, and installs the same replacement token as both API-host and domain-scoped HttpOnly cookies so retained-cookie ordering cannot separate the workspace from its principal. Page JavaScript receives only user/workspace IDs. Broader verification, recovery, enrollment, rate governance, and versioned account lifecycle remain incomplete. See [decision 0050](../decisions/0050-fresh-browser-identity-bootstrap.md). |
| Clean account-passkey lifecycle | Implemented and deployed to development | `onevar-platform` | Development release `04e1e9495c42b29e7cb76d1b9c759e2b4e2f5c8d` lets one exact entity account retain up to ten independently labeled discoverable passkeys. Every credential keeps exact ID, WebAuthn public key, RP/origin, signature counter, status, and lifecycle; friendly labels plus a short entity suffix are interaction addresses only. Authenticated commands add, list, and revoke exact credentials without asking users to copy IDs, while refusing to revoke the final active credential. New DynamoDB rows are independently keyed and indexed; the former singleton row remains readable as a legacy credential so deployment does not strand existing accounts. Account authentication grants no Protected Asset authority, and 1var cannot recover private passkey material absent from every authenticator. Recovery without a surviving session or authenticator remains incomplete. The full repository gate and a two-browser lost-original/add-replacement/sign-in/revoke journey pass; the deployed route and lazy command artifact were verified without resetting development state. See [decisions 0084](../decisions/0084-account-passkeys-are-not-protected-authority.md) and [0097](../decisions/0097-account-passkeys-are-multiple-labeled-credentials.md). |
| Email verification | Implemented foundation | `aws` + `compute` | Separate request/poll/verification flow exists; integration with account state requires formalization. |
| Headless API/Compute acceptance | Implemented foundation | `testing` + `aws-api` + `compute` | Command transport, session capture, test-device keys, mailbox parsing, declarative scenarios, and reset guards exist. Provisioned isolated stacks, mailbox infrastructure, and full live acceptance remain deployment work. See [headless acceptance testing](headless-acceptance-testing.md). |
| Authorized test-environment reset | Implemented foundation | `aws` + `testing` + `compute` | Clients and server fail closed; the portal explicitly requests canonical mode, and a durable marker forces a one-time legacy purge before the first canonical reset. Reset is a step-numbered control-table job that deletes one bounded page per signed continuation, avoiding gateway timeouts and duplicate advancement on retry. Active compatibility outputs remain recurring cleanup until cutover. Explicitly configured non-production deployments may use an authenticated-user allow-list or temporary any-caller mode. Parallel isolation remains future work. |
| Clean-room private hard reset | Implemented and live-proven in development; pre-launch production deployment pending | `onevar-platform` | A private Standard Step Functions workflow advances a durable stage reset epoch, purges pending Stripe and schedule queues, and deletes bounded pages from the complete runtime DynamoDB table plus generated capability-package and ordinary-artifact S3 prefixes until all are empty. A local script starts it through an exact-user, MFA-gated IAM role; there is no public route or Function URL. Development workflow [33297907699](https://github.com/jaustinhughesgit/onevar-platform/actions/runs/33297907699) proved the ordinary-artifact extension through its reset-gated deployed suite. Production synthesis requires an explicit temporary pre-launch flag and distinct operator confirmation, which must be removed before general availability. Stripe-side history remains untouched, while verified events created before the cutoff are ignored by the new runtime epoch. Code, infrastructure, core catalog, policies, secrets, webhook destinations, and browser-only protected files remain. See [decisions 0074](../decisions/0074-private-development-hard-reset.md), [0107](../decisions/0107-prelaunch-resets-advance-an-external-event-cutoff.md), and [0114](../decisions/0114-ordinary-artifacts-separate-bytes-authority-and-execution.md). |
| Device encryption/signing keys | Implemented foundation | `aws` + `compute` | Browser generates device-held P-256 material and registers public keys; rotation/recovery need contracts. |
| WebAuthn enrollment | Implemented and deployed for exact protected Compute, presentation, and recipient operations | `onevar-platform` + POC evidence in `aws` + `compute` | Releases `391d91d5fb89e226db40c6e3034f97941c9f14fa` and `cfd0e2383c4353d582ad44c940b651dfad65b47b` verify exact RP/origin, UV/UP, attested credential ID, ES256 COSE/SPKI identity, assertion signature, and counter. Enrollment grants nothing. Discriminated targets bind Reveal, Speak, owner share approval, and recipient decryption to principal/binding/asset/request/grant/key versions without a value channel. Development workflow `32896626867` and production workflow `32897153279` each passed 19 runnable reset-gated deployed scenarios. Recovery and trusted-server policies remain incomplete. See [decision 0073](../decisions/0073-hardware-gated-presentation-and-recipient-use.md). |
| Local Path signature matching | Implemented | `aws` | Structural and exact matching exist. Quality-gated bundled Paths install locally before server replication, so Compute outages do not block the browser foundation; generalization, conflict resolution, and durable retry remain active work. |
| Compositional subpattern capability framework | Implemented foundation | `aws` + `aws-api` + `compute` | Pattern Schema v4 composes shared typed role, relation, operator, modifier, projection, and syntax subpatterns into the same locally validated Path/Essence contract. Named capability packs account for installed semantic operations and existing integration boundaries. Quantity and event-count migrations replace nineteen active sentence-shaped equations with four composed Paths, including coordinated entity-set quantities. Additional generic compositions handle `my <property> <copula> <value>` and unquantified `<actor> <have> a <thing>` possession without domain vocabulary. An owned-entity alias transaction adds a compound name and its component mentions to one uniquely resolved existing object, while graph-aware possessive assertions and status/choice projections reuse that identity. A named-owner choice composition resolves `<person>'s <object kind>` through an authorized direct relation and projects the related object's current property after exact-name hydration. The merged foundation has 22 installed Paths. Server replication is byte-bounded, preserves splittable payload-size errors, and validates the v4 persistence envelope without moving matching authority out of the browser. Other bundled families remain v3, and content-addressed lazy pack hydration is future work. See [decisions 0020](../decisions/0020-compositional-subpattern-capability-framework.md), [0022](../decisions/0022-public-profile-named-context-hydration.md), and [0047](../decisions/0047-owned-entity-aliases-preserve-local-identity.md). |
| Confirmed Path foundation promotion | Implemented foundation | `aws` + `compute` | An authorized reviewer can promote a browser-tested exact Path or alias with its originating sentence into a retained reset-exempt foundation store; every identity hydrates it automatically. Staged release, revocation, and production publisher governance remain lifecycle work. See [decision 0010](../decisions/0010-confirmed-path-foundation-promotion.md). |
| Browser-local canonical Essence generation | Implemented foundation | `aws` | Installed Paths materialize validated local transforms. A cold miss must install and replay a locally validated Path before an Essence can execute; browser execution does not fall back to model-generated Essence. Coverage and formal schemas remain incomplete. |
| Essence query and mutation | Partial | `aws` | Statement/query flows exist; modifiers, referents, and repair reliability need formal schemas. |
| Compute-declared ContextDB effects | Implemented foundation | `aws` + `compute` + `testing` | A capability can declare one bounded scalar relation-object replacement. Compute validates the manifest and returns a typed value; the browser uniquely resolves the mentioned subject through its connected values, proves the old state, applies the local rewire, and treats an already-applied effect idempotently. Fixed effect state belongs to the effect contract: manifest and EntityPlan reconciliation remove an unspoken generated duplicate current/result input before JPL compilation even when its binding source is mislabeled, while preserving explicitly spoken transition values. One-variable effect command families also discard unused invented inputs regardless of generated binding-source label, so a constant operation selector cannot become a required `action` input; explicitly declared parameters remain. Mutating operations now expose exact app/version/operation-scoped logical dependency IDs. During another user's new-Path creation, discovery may reconcile those IDs against up to 20 recent ordinary inputs and 200 word-selected ordinary entities, but its `using` proposal must select exact supplied target subject/property/relation IDs. The user-owned installed Path pins that binding, runtime rewires only that relation, and Context publication remaps both temporary entity and relation IDs through the Path before sync. Publication acknowledgement is ordered with hydration installation so an older hydration response cannot restore a temporary duplicate after canonical remapping; an exact relation rewire or tombstone remains authoritative while its publication is pending, even if hydration began before acknowledgement remapped its ID. A later rewire of an acknowledged relation preserves that canonical relation identity only after authenticated publisher ownership is verified, so the new object versions the relation instead of coexisting with its old value. Creator Paths, data, and protected evidence are never copied. New target allocation, broader dependency kinds, multi-relation transactions, and rollback across external effects remain incomplete. See [decision 0046](../decisions/0046-browser-applied-compute-context-effects.md) and [decision 0048](../decisions/0048-compute-installations-bind-entity-use-by-exact-id.md). |
| Browser-local entity relationship explorer | Implemented foundation | `aws` | Entity Graph consumes the protected ContextDB presentation snapshot and renders bounded subject → property → object neighborhoods as directed SVG connections. Search, focus history, node navigation, zoom, pan, depth, and node limits operate entirely in the browser; clicking a property treats it as the same first-class entity represented by the Essence relation. The module neither fetches a parallel graph nor reveals Protected Asset plaintext. Dense-graph clustering, saved layouts, temporal/version overlays, and broader accessibility/device proof remain work. |
| Browser-local derived arithmetic and counting | Implemented foundation | `aws` | Read-only Essence rows support deterministic add/subtract/multiply/divide, distinct identity counting, signed summation over bound records, and property/default aggregation in which each distinct record contributes its explicit numeric property or a declarative fallback only when the property is absent. Unit algebra, ambiguity clarification, and a fully versioned schema remain incomplete. |
| Deterministic Path response sentences | Implemented foundation | `aws` + `onevar-platform` | Every bundled answer-producing question operation carries a catalog-owned template over locally proven query variables and request bindings while preserving the canonical raw answer. Hydration backfills matching existing direct and semantic Paths without overwriting learned presentation. If another successful ordinary Path lacks a template, a resumable model job may propose one over bounded proven variables; server, browser, and Path Builder validation precede installation and current-response use. Voice-originated proven ordinary answers become ephemeral local Automation speech steps using configured OpenAI speech first. The initiating press prepares reusable Web Audio. Exact ordinary response text may begin transient synthesis before remaining presentation work finishes, but playback releases only after the same accepted interaction has committed its required effects; failed, superseded, or interrupted work aborts the preparation. Browser synthesis is failure-only fallback. The development voice policy uses `tts-1` with `nova`. Typed responses remain silent. Protected responses bypass server speech and synthesize only after local authorization/decryption. Broader grammar and localization remain future work. See [decision 0016](../decisions/0016-path-response-sentences-from-local-proof.md), [decision 0017](../decisions/0017-missing-response-templates-use-locally-validated-llm-proposals.md), and [decision 0124](../decisions/0124-interaction-automations-are-local-runs-and-protected-speech-never-leaves-the-device.md). |
| Data-defined local semantic entities | Implemented foundation | `aws` | Versioned operation templates compile locally with Path bindings. Personal and qualified-owner quantities share signed observation storage. Quantity, participant-event, vocabulary-neutral activity occurrence, relationship transition, direct entity property, entity classification, selected-relation correction, and local trigger/effect policy entities exist. Participant-event assertions retain optional multiplicity; actor counts and typed-participant counts sum that explicit value while treating an unquantified distinct record as one. Role-aware supporting-graph routing can select the participant projection only when executed question/support bindings identify the same entity. Coordinated entity lists preserve canonical identity separately from source display form and can render a natural-language list. Relationship transition separates an observed transition, object/reference identity, and a resulting relationship between distinct objects; its catalog-declared binding constraints reject overlapping or ungrounded semantic roles, and catalog-declared failure routes can redirect repair to a compatible primitive. Direct entity property records one subject-property-value state with optional provenance and typed identity details. Classification can add a broader type to a specific entity without domain vocabulary. Correction provenance/version history, transitive ontology inference, contradictions, state expiration/reversal, and formal contract schemas remain incomplete. |
| Local syntax evidence + LLM semantic adjudication | Partial | `aws` + model services | The browser extracts vocabulary-neutral structural triplet evidence. A cold miss supplies it with bounded recent results and ContextDB to a strict model response containing speech act, answer or graph writes, grounded historical repairs, and Path plans. The complete adjudication is returned to the browser on success and rejection. A separate labeled `interpretation` envelope makes the model answer, explanation, correction status, sanitized response, and usage observable without treating them as locally verified truth. Catalog-declared local-syntax plans can compile typed alternatives for compatible grammatical structures. Catalog-authorized structural-token policies may prove fixed grammar only when their selected operation effect is locally satisfied; ordinary semantic values still require typed bindings. The browser requires every accepted Path to materialize the rows or reproduce the answer atomically; the model has no direct write authority. Compatible installed question Paths are evaluated read-only as a guarded set: one answer-bearing semantic interpretation wins, while multiple valid interpretations fail closed and their bounded evidence reaches repair. Repeated identical deterministic failures stop further model escalation. Contradiction policy, user-facing ambiguity confirmation, and broad cold-domain evaluation remain incomplete. See [decision 0012](../decisions/0012-llm-semantic-adjudication-to-locally-proven-paths.md), [decision 0015](../decisions/0015-visible-llm-interpretation-with-local-proof.md), and [decision 0018](../decisions/0018-catalog-authorized-structural-evidence-in-path-repair.md). |
| Typed Semantic Plan → Path synthesis | Implemented foundation | `aws` | Model-selected semantic operations and grounded roles are compiled deterministically into matchers and catalog-owned executable transforms. Semantic Plan v2 accepts role sources/spans without model-authored runtime value modes when an operation publishes `bindingSchemas`; catalog defaults, dependencies, and source policies complete the binding contract. Versioned support-binding derivations can expand one query plan into an atomic historical-repair/query transaction. Rejections identify interpretation, compiler, catalog, or validation ownership so deterministic compiler/catalog defects do not consume more model correction rounds. Declarative non-catalog Essence Paths remain available when no installed operation faithfully represents the adjudicated graph, subject to browser validation. Broader binding-schema adoption, automatic versioned capability induction, and complete capability-gap remediation remain work. See [decision 0011](../decisions/0011-typed-semantic-plan-path-synthesis.md) and [decision 0019](../decisions/0019-semantic-role-plans-use-catalog-binding-schemas.md). |
| Sentence/Essence/signature/Path runtime | Implemented foundation / partial spec | `aws` | End-to-end interaction chain exists; versioned schemas and generalized repair remain active work. |
| Command registry, menus, and calls | Implemented foundation | `aws` + entity JPL | Declarative commands and state transitions exist; target/permission schemas need formalization. |
| Clean-room app menu and focused composition | Implemented and development-proven; equivalent-build deployment proof pending; collaboration transport partial | `onevar-platform` | Successful Convert builds and first-used shared packages become user-local menu entries over immutable app bundles. Natural open/show/close/select/lifecycle/fork commands resolve without copied IDs; exact IDs still own focus and execution. Open-app frames precede the rest of the local menu and global discovery. Repeated equivalent plain root-only builds owned by the same local user retain distinct immutable capability packages but advance one stable app bundle lineage and one active menu projection. Equivalence requires normalized operation-contract plus Invocation Frame agreement or substantial locally retained repeated-Convert evidence; publisher, marketplace, unrelated-request, supporting-entity, and ArrayLogic differences remain ambiguous. Legacy duplicates repair to the newest equivalent release on open. Double-tap Convert and `improve app:` share one intent envelope and bounded 20/200 ordinary context. The first general focused addition compiles browser-local event record plus count/sum/filter/group primitives into sequential ArrayLogic; root effect commit precedes recording, and server JPL rejects the local plane. Builders have local write; shared first-use entries are use-only; a fork owns a new composition without mutating the shared source. Exact verified grants may delegate read/use/write for one owner, grantee, app anchor, and root release but never delete or ownership. Higher revocation revisions remove write; exact capability revocation disables installations, Paths, workflows, focus, and pending work without erasing evidence. Independent-device and failed-edit rollback tests pass. Authenticated cross-user grant delivery, same-account cross-device menu sync, and user-facing Undo/Back remain incomplete. Development release `9f8306b` passed the one-response live Convert/menu/edit/event proof in run `33038391956`; governance release `c53f49b` then passed all 21 runnable deployed scenarios in run `33039344684`. Both concluded with independently verified zero-record hard resets. See [decisions 0081](../decisions/0081-local-app-menu-and-browser-aggregate-composition.md), [0121](../decisions/0121-equivalent-owner-convert-builds-advance-one-app-lineage.md), and [0082](../decisions/0082-exact-app-collaboration-and-release-revocation.md). |
| Entity automation and sequences | Implemented bounded Interaction Automation foundation | `onevar-platform` + entity JPL | Immutable local definitions and separate bounded run state execute up to 64 ordered speech/ordinary-input steps from zero through fifteen minutes in a dedicated browser worker. Refresh resumes pending local runs, cancellation is explicit, microphone capture interrupts playback, and longer or absent-browser work stays in governed schedules. Arbitrary callbacks and protected server speech are structurally excluded. Broader reusable game/timer templates and additional typed actions remain work. See [decision 0124](../decisions/0124-interaction-automations-are-local-runs-and-protected-speech-never-leaves-the-device.md). |
| Local ContextDB | Partial | `aws` | Graph storage and retrieval exist; persistence and refresh behavior require an explicit storage specification. |
| Detached word and lexical addressing | Implemented foundation / partial spec | `compute` + portal | Compact Words records and normalized lookup indexes are distinct from entity IDs; subdomains can reference word IDs and reverse-address matching records. The logical v1 Word contract now exists; full producer adoption, action-level authorization, pagination, sense handling, and scale benchmarks remain incomplete. See [decision 0023](../decisions/0023-words-are-lexical-addresses.md). |
| Canonical entity-substrate contract and persistence port | Implemented foundation | `architecture` + `compute` | Logical v1 records cover Words, entities, addresses, groups, typed relations, grants, capabilities, installations, local mappings, mutations, and candidate-only retrieval postings. New Context writes compile canonical records and sharded projections behind the port; hydration dual-reads and authorizes canonical records. Bounded backfill, graph parity, cutover/rollback, and scale/security gates now exist; deployed backfill, canonical-only activation, retirement, and production-scale proof remain operations. See [canonical entity substrate](canonical-entity-substrate.md), [migration](canonical-substrate-migration.md), and [decision 0026](../decisions/0026-sharded-canonical-context-publication-and-hydration.md). |
| Lemma, morphology, and alias lexical graph | Product intent | Cross-layer | Word roots, inflections, aliases, language, and morphology should reduce duplicated wording while preserving entity and semantic-sense identity. Query planning and migration are not implemented as one canonical contract. |
| Local entity → server entity publication | Partial | `aws` + `aws-api` + `compute` | The active transcription worker records ordinary graph deltas in encrypted local state, publishes asynchronously with idempotency, and applies authoritative IDs across browser state. Compute now compiles participant/public records into canonical Words/entities/addresses/Context groups/links/versions/grants before its retained sidecar write; hydration dual-reads canonical plus old sidecar records. Explicit delegated grants, conflict arbitration, historical backfill, and sidecar retirement remain work. See [distributed entities](capabilities/distributed-entities.md) and [decision 0026](../decisions/0026-sharded-canonical-context-publication-and-hydration.md). |
| Shared hard-data entities | Implemented foundation / partial breadth | Cross-layer | New ordinary Context facts publish into canonical entity/link records and grant-checked participant/profile hydration can read them. Coordinated named/current-speaker quantity queries bind an authorized entity set and aggregate distinct observation records locally. Other aggregate families, legacy creator migration, delegated-grant UX, backfill, and cross-browser production proof are not complete. |
| Server entity hydration | Partial | `aws` + `aws-api` + `compute` | Startup and periodic hydration read only the authenticated principal's audience partition, merge shared nodes/relations into ContextDB, and bind the principal's stable entity as `speaker`. Explicit named cross-user questions exact-refresh the current authorized public component even when the identity is already local; remembered IDs avoid another candidate search but do not stand in for freshness. A two-profile command scenario proves the chronological refresh contract. Browser-worker production coverage and incoming-fact trust UX remain. |
| Required and expanded Path learning | Partial | `aws` + model services | A cold miss performs semantic adjudication and builds current plus grounded historical repair plans; deterministic synthesis and the browser tournament validate, install, and replay them before graph execution. Candidate checks distinguish syntactic token coverage from typed semantic coverage. Catalog-backed statements enforce compiler-canonical unconditional graph rows while retaining raw model rows as diagnostic intent; conditional graph enrichments execute only when their optional bindings exist. Questions use the adjudicated answer as a local proof target. Compatible new wording extends an existing semantic family as a tested alias; morphology and collision cases belong to Path tests, while direction-sensitive wording remains interpretation work. A structurally matched Path that fails its runtime contract carries separate typed failed-match evidence through repair without becoming successful coverage; catalog routes can compare those question roles with exact executed bindings from prior statements and make the local graph authoritative for the routed proof. Supporting-source selection can also use bounded adjacency from recent typed entity bindings, and catalog conditions can refine a generic historical role from the current query inside the atomic proof. A uniquely closest prior statement supplies a vocabulary-neutral graph-continuity constraint, preventing a learned continuation from moving the same semantic occurrence into an incompatible ContextDB predicate family. Auto-build separately permits paraphrase signatures. General ontology inference, ambiguity confirmation, and broader dependency/capability-gap contracts remain incomplete. |
| Intent jurisdiction: data, invocation, composition, or capability creation | Implemented foundation | `aws` + `compute` | Browser results and Compute discovery emit intent-jurisdiction v1 with typed effect class, artifact decision, evolution outcome, stable reason, and target evidence. Local graph work cannot become Compute merely because a Path is missing. Composition classification, complete authority evidence, and all legacy adoption remain incomplete. See [intent routing and entity evolution](intent-routing-and-entity-evolution.md). |
| Entity parent/child middleware lineage | Implemented Phase 5C runtime boundary; deployed promotion pending | `architecture` + `onevar-platform` | Middleware freezes deterministic selected-target-to-root owning order, exact browser/file/Compute transport authority per node, per-node `use` authorization, bounded traces, and `pass`/`respond`/`fail` first-response termination. A child can specialize or answer before its parent fallback; missing or duplicate transport bindings fail closed. See [middleware/composition/governance](entity-middleware-composition-and-governance.md), [decision 0063](../decisions/0063-target-first-entity-middleware-order.md), and [0110](../decisions/0110-advanced-execution-is-signed-exact-and-explicitly-reversible.md). |
| Entity materialized output | Implemented legacy foundation / partial contract | `compute` + portal | Entity/subdomain output can provide a stored fast response and some Shorthand flows write materialized output. Cache keys, inputs, dependencies, freshness, invalidation, version changes, and permission revocation are not one formal contract. |
| `map` | Implemented foundation | `compute` | Typed non-owning scoped member routing with canonical endpoint authorization and conformance writes. |
| `extend` | Implemented foundation | `compute` | Typed owning parent/derived-child composition; the only one of the five primitives that contributes ordinary middleware ancestry. |
| `link` | Implemented foundation | `compute` | Typed non-owning reference between independently governed entities. |
| `use` | Implemented foundation | `compute` | Typed non-owning incorporation of exposed members or behavior without identity transfer. |
| `substitute` | Implemented foundation | `compute` | Typed non-owning replacement within an explicit caller scope. |
| Public/private entity visibility | Partial | `compute` + portal | Exists; full interaction with authorization requires specification. |
| Canonical action authorization and lifecycle | Implemented foundation / partial adoption | `architecture` + `compute` | One decision covers owner, action grants, expiry, public-read limits, optimistic versions, revocation/deletion, and exact legacy compatibility evidence. Lifecycle state, version evidence, and sanitized audit can commit atomically. Protected Assets retain separate cryptographic/consent checks; non-composition consumer migration remains. |
| Search and reuse of others' work | Implemented, deployed, and clean-reset proven | `onevar-platform` | Portable app definitions exclude creator-local Paths, bindings, and installation IDs. The API verifies content-addressed packages, signs passkey-authenticated publisher/review/provenance/compatibility/rollout/pricing/lifecycle evidence with a stage KMS P-256 key, and indexes bounded term postings. A recipient verifies the signature and package/workflow hashes and compiles fresh local runtime identities. Exact licenses support public install/use, governed refund/reinstall, use-only and write collaboration, collaborator-authored successor releases without ownership transfer, name-addressed revocation, caller-owned licensed forks, upgrades, lazy rollback, deprecation, discoverable-passkey same-account release deltas, and abuse intake. Revoked local Invocation Frames fail explicitly instead of falling through to unrelated discovery, and publication projects bounded discovery tags from longer immutable aliases. Account authentication remains separate from protected authority. Release `4ec2f11203201c0ac23d68498f92642d728c61b3` passed complete verification, 29 local browser scenarios, a 64-recipient exact-install concurrency gate, development workflow `33194741655`, its live six-account lifecycle canary, and identical-SHA production workflow `33195357790`. The final private development reset deleted 1,667 records and 14 generated artifacts; an independent inventory observed zero of each. Human/business identity verification, operational moderation/appeals, and projected million-DAU performance/chaos proof remain Phase 5 work rather than claims of this bounded Phase 4 closure. See [decisions 0083](../decisions/0083-signed-portable-marketplace-releases.md) and [0084](../decisions/0084-account-passkeys-are-not-protected-authority.md). |
| Hybrid referent resolution and Path-scoped entity memory | Implemented complete-path/minimum-question foundation / partial global graph ranking | `aws` + `compute` + `onevar-platform` | Qualified referents resolve from local ContextDB, exact identity-scoped memory, recent hydrated IDs, and authorized public Context. The clean runtime now filters same-name candidates by the complete requested ordinary-object or protected-binding path before ranking; one survivor executes, while unresolved ties return at most five safe public connected descriptions and remember the user's exact selection locally. DynamoDB name postings use 64 deterministic owner shards and bounded reads, and exact memory bypasses cold fan-out. Capability Search receives the behavior phrase without the concrete owner. Arbitrary multi-hop six-degree Position tournaments, learned decay/correction scoring, generalized non-person paths, and effect-risk thresholds remain work. See [decisions 0041](../decisions/0041-path-scoped-referent-memory.md) and [0116](../decisions/0116-authorized-referent-positioning-ranks-complete-paths.md). |
| Payments/marketplace | Phase 5.1 engineering complete in development sandbox; production activation externally gated | `onevar-platform` | The production double-entry ledger is separate from conformance credits and now owns Checkout fulfillment, marketplace and reviewed-provider charges, refunds, publisher accrual, rolling reserves, Connect onboarding, exact transfers, payout evidence, remote transcription, authoring, JPL, and scheduled execution. One remote interaction root receives at most one receipt and debit. Standard work at or below five seconds and 5,000 tokens uses the prior final 5 AM block's frozen average; heavy work records itemized model usage and immutable Lambda request/duration/memory/architecture/region/response-byte pricing. Heavy evidence is excluded from the next ordinary average. Missing usage or policy fails closed. Production provider execution no longer writes a shadow test-credit ledger. Every currently admitted provider explicitly declares the exact 1var-paid USD amount, including zero for no-network or user-funded quota, and catalog admission without that field degrades coverage. A private six-hour reconciler stores tagged Cost Explorer evidence, while exact heavy meters provide immediate partial compute/bandwidth coverage; an opt-in deploy gate invokes it and surfaces its exact failure. Separately signed platform and Connect Stripe events enter an encrypted durable inbox, retry boundedly, and retain exact money identities. Checkout API evidence and signed events converge idempotently; refunds reverse exact issuance; publisher earnings preserve cash provenance, reserve lots, pending-settlement retries, transfers, and bank-payout evidence. One stage-level worker—not every browser—retries due withdrawals with immutable Stripe idempotency. `billing costs` exposes evidence and `billing readiness` distinguishes loaded runtime contracts from external approval. Production mode cannot synthesize without explicit product, model, server, payout, and currency policies, and cannot use sandbox Stripe mode. Live activation remains blocked on Stripe/legal approval, approved production policies, live credentials, canary promotion, moderation, and production scale proof—not missing Phase 5.1 runtime contracts. See [decisions 0085](../decisions/0085-production-billing-and-protected-lifecycle-remain-separate-trust-planes.md), [0086](../decisions/0086-publisher-credit-accrual-precedes-connect-transfer.md), [0088](../decisions/0088-checkout-return-and-webhook-share-one-fulfillment.md), [0089](../decisions/0089-cost-meters-retain-usage-and-versioned-pricing.md), [0090](../decisions/0090-cost-pricing-and-user-transfer-fees-are-versioned-separate-policies.md), [0091](../decisions/0091-cost-source-coverage-and-external-fee-evidence.md), [0092](../decisions/0092-two-lane-daily-interaction-pricing.md), [0095](../decisions/0095-marketplace-currency-selects-an-exact-ledger.md), [0096](../decisions/0096-versioned-promotional-credits-use-production-ledger.md), [0098](../decisions/0098-shared-cost-reconciliation-and-local-protected-composition.md), [0099](../decisions/0099-stripe-refunds-reverse-exact-credit-issuance.md), [0100](../decisions/0100-stripe-money-events-use-a-bounded-durable-inbox.md), [0101](../decisions/0101-publisher-withdrawals-separate-reserves-transfers-and-payouts.md), [0102](../decisions/0102-credit-provenance-gates-publisher-cash.md), [0103](../decisions/0103-publisher-transfers-link-exact-checkout-charge.md), [0104](../decisions/0104-one-root-settlement-and-heavy-server-attribution.md), and [0108](../decisions/0108-publisher-withdrawals-retry-after-settlement.md). |
| JPL entity execution | Implemented / partial spec | `compute` | `published.actions` executes sequentially using the entity context and declared modules; the complete versioned schema and semantics are incomplete. See [execution representations](execution-representations.md). |
| Shorthand matrix execution and entity composition | Implemented / partial spec | `compute` | Active runtime executes row references, transforms, physical/virtual segments, and route calls; canonical keyword contracts, side-effect rules, and versioning are incomplete. See [execution representations](execution-representations.md). |
| ArrayLogic conversion/build plans | Implemented sequential runtime with explicit compensation; broader authoring partial | `compute` | Exact capability/version/operation steps execute sequentially. Phase 5C admits `explicit_reverse` only when every step declares an exact authored inverse; failure executes those inverses in reverse commit order and records incomplete compensation rather than inferring rollback. Convert authoring UI for arbitrary inverse contracts remains partial. See [execution representations](execution-representations.md) and [decision 0110](../decisions/0110-advanced-execution-is-signed-exact-and-explicitly-reversible.md). |
| Entity creation with structured model output | Partial | `compute` | Schema/validation paths exist; generation and repair reliability remain active work. |
| Entity + Path + context-aware editing | Partial | `aws` + `compute` | Semantic bundles and repair targets exist; transactional correction and replay require continued hardening. |
| Capability reuse, repair, fork, installation, and promotion | Implemented decision/index foundation / partial lifecycle | Cross-layer | Discovery emits typed reuse/repair/fork/build decisions: an existing declared operation failure is repair, while a new operation is fork. Validated manifests now receive an exact contract fingerprint and a sanitized semantic Position; Convert/Search narrows the catalog to authorized `use` candidates before exact manifest reload and model adjudication. Convert retries initial Position projection and cannot report `BUILT_AND_REGISTERED` until that reusable posting is acknowledged. Public visibility alone does not authorize execution; a reusable public definition has an explicit `pub` use grant. Owner, configuration, local Paths, and Protected Assets remain separate. Legacy `extend` remains as a compatibility field. Existing-definition backfill, immutable releases, fork persistence/lineage, first-class installation records, exact reranking, promotion, and dependency compatibility remain incomplete. See [intent routing and entity evolution](intent-routing-and-entity-evolution.md) and [decision 0039](../decisions/0039-compute-contract-fingerprints-and-positioning.md). |
| Compute capability discovery/build/install | Partial | `aws` + `compute` | Background discovery and generation exist. Build discovery now freezes a structured semantic answer plan before accepting the generated capability contract; Compute reconciles ContextDB address ownership and operation/input/output identity, permits one validation-informed background correction, and fails closed after a second mismatch. Entity Plans may use zero provider requests when they only transform or present browser-resolved ordinary inputs; ContextDB and other binding sources remain local resolvers rather than invented HTTP providers. Repeated direct-property observations resolve newest-first at browser invocation, preserving older graph history without letting it shadow the current value. External-provider construction may research primary documentation, Entity Plans can compose an earlier provider response into a later request, and rejected provider plans constrain later research to the selected provider domains before deterministic validation. Typed two-operand calculations compile directly into trusted Math JPL, avoiding provider/model generation and example-literal leakage. New/edited manifests are fingerprinted and positioned; indexed discovery is now an implementation foundation. Reuse shares the exact authorized manifest rather than the creator's Path: the current caller dynamically compiles a separate local Path and can pin validated app-scoped entity-use bindings to exact ordinary graph IDs. An unvalidated one-slot `entity_reference` remains open beyond its examples: when a new browser-resolved referent preserves an exact declared invocation frame, an erroneous model extension decision is reconciled to reuse; closed validation domains and behavior changes remain extensions. Existing-definition backfill, exact reranking, generalized installation records, target allocation, and production-scale proof remain active work. Browser installation fails closed until the Transcribe worker acknowledges a request-correlated revision containing every newly compiled compute Path signature; only that acknowledgement unlocks invocation replay, and review bundles retain Convert/Essence origin plus build/install/replay provenance. Unverified Path-repair interpretations remain labeled Message diagnostics and do not appear as successful interaction-rail responses. Capability materialization has a single-finalizer lease, including deterministic Shorthand builds before they enter materialization; its worker lifetime exceeds the observed materialization window and the lease exceeds that worker lifetime, while provider calls retain smaller deadlines. Terminal polls retain bounded failure diagnostics, and `BUILD_IN_PROGRESS` returns the exact coordinator ID so a client that lost the first build response resumes the same record. If that response ended before the client learned an ID, Compute reconciles the exact owner/capability/request-hash record before registry collision handling and returns the running or completed build rather than a false extension conflict. A poll for the originating successful build remains `BUILT_AND_REGISTERED` with bounded ArrayLogic/Shorthand/JPL evidence, and an approved child capability can materialize without a readable parent workspace document. `CAPABILITY_REUSED` remains reserved for a request that selected a definition it did not just create. Nested Compute failures retain a sanitized typed diagnostic across an HTTP-success relay. A capability build that remains queued or in progress beyond its server-owned coordinator lifetime becomes a retryable `OPENAI_BACKGROUND_RESPONSE_STALLED` terminal failure; discovery separately normalizes seconds, milliseconds, and ISO provider timestamps and rejects missing timestamps. Browser and headless controllers start at most two fresh replacements instead of polling forever or returning `UNKNOWN`. An unclassified, compute-eligible cold question or command asks capability jurisdiction before local compilation. A classified command processing miss also asks capability jurisdiction before local graph repair even when the miss is labeled as new syntax, because the action first requires an executable operation contract; explicit local graph question misses remain local. A `not_compute` decision receives one bounded local proof attempt; if that proof fails, jurisdiction reconsideration receives the bounded local failure interpretation and may build/reuse external behavior rather than repeatedly proposing a non-executable local query. Durable server-owned continuation, broad protocol reuse, generalized installations, target allocation, and broader input binding remain active work. See [decisions 0031](../decisions/0031-lease-compute-build-finalization.md), [0040](../decisions/0040-browser-resolved-inputs-are-not-providers.md), [0044](../decisions/0044-semantic-answer-plans-precede-compute-contracts.md), and [0048](../decisions/0048-compute-installations-bind-entity-use-by-exact-id.md). |
| Compute answer verification | Implemented opt-in / partial | `aws` + `compute` | Verification toggle exists; history scope, cost policy, and durable behavior should be specified. |
| Failure diagnosis | Partial | `aws` + `compute` | The browser now assigns stable capture-through-presentation levels, owners, codes, retryability, model eligibility, and repair actions. Capability Lab clusters these fingerprints and compares locally verified coverage between runs. Compute/provider adoption and one complete cross-layer diagnostic contract remain incomplete. See [decision 0020](../decisions/0020-compositional-subpattern-capability-framework.md). |
| Per-request model cost inspection | Implemented foundation | `aws` + `compute` | Browser session ledger expands each request into sanitized model attempts and dated USD estimates; broader non-token provider/tool charges and billing reconciliation remain future work. |
| Versioned LLM request templates | Implemented foundation | `aws` + `compute` | Message dropdown selects Original or New; trusted registries preserve one template through the request lineage and Cost labels the treatment. Representative quality/cost evals remain ongoing. |
| Background OpenAI jobs | Partial | `compute` + `aws` | Durable response/job mechanisms exist in code. Required browser Path construction and its model/runtime correction rounds use resumable response IDs rather than holding an API Gateway request open; comprehensive idempotency and lifecycle contracts remain incomplete. |
| Scheduled entity tasks | Phase 5.4 and scalable v2 dispatch implemented, deployed, and low-volume live-proven in development | `onevar-platform` | A browser resolves language once, then freezes exact installation/capability/version/operation/package/JPL/input/price identity. Versioned minute-bucketed compact due pointers across 64 virtual shards, bounded cursor catch-up, a constant-work EventBridge coordinator, FIFO shard workers, a separately buffered occurrence queue, partial-batch retry/DLQ, and deterministic occurrence/invocation IDs drive the normal Compute/provider lifecycle without a model call per run or a scan proportional to stored schedules. Authoritative rows and old/new pointers change atomically; owner history uses bounded chronological pointers; operational occurrences expire after 90 days. Runtime rechecks release, price, credits, and exact protected grants; missing authority pauses. Ordinary results rejoin the exact local installation. Phase 5.4 adds IANA-timezone daily, weekday, weekly, and monthly calendar rules, bounded immutable definition history, actionable-state natural lifecycle commands, and complete ordinary ArrayLogic targets. Workflow occurrences wait for an eligible owner browser, execute through the exact local workflow installation against browser ContextDB, and commit only a value-free receipt with deterministic retry identity. Definition version remains separate from operational revision, and occurrences pin both. Release `f5491f60671fe8e50064221bc99341f9c1eb3196` and workflow [33292749505](https://github.com/jaustinhughesgit/onevar-platform/actions/runs/33292749505) passed the full repository gate, reset-gated deployed browser acceptance, CloudFormation promotion of the scalable workers/queues, and the real EventBridge/SQS governed-schedule canary. Deterministic v2 tests separately prove three independent same-minute schedules through claim, execution, completion, and acknowledgement. Sustained large-load certification, downstream channels, protected/provider workflow per-occurrence authority, protected local execution while absent, and production deployment remain incomplete. See [decisions 0077](../decisions/0077-clean-governed-schedules-pin-exact-compute.md), [0111](../decisions/0111-calendar-schedules-use-zoned-rules-and-immutable-definitions.md), [0112](../decisions/0112-scheduled-arraylogic-retains-browser-context-authority.md), and [0113](../decisions/0113-time-bucketed-due-pointers-scale-schedule-dispatch.md). |
| Browser-authoritative event synchronization | Implemented and live-proven in development; fixed-analytics correction deployed | `onevar-platform` | One authenticated delta carries pending owner schedule occurrences. Light, medium, heavy, and real-time modes make freshness/cost explicit; one visible leader tab owns intervals or the WebSocket, while hidden/follower tabs do not poll. A WebSocket carries only a constant change hint and cannot transport or apply Context/protected data. Active browsers automatically deliver scheduled results through the exact installation; manual `schedule results` remains diagnostic. Release `06925e6a1dbadbf794a8c87b2f0a2b48cdb1d0bd` removes the earlier fixed publisher-analytics event, rows, cursor, and command because analytics must be an explicit user-authored data/query capability. The worker stores exact Compute/ArrayLogic authoring focus; `add step` and contract-equivalent `fix step` create immutable workflow releases. Workflow `33031904635` passed verification and 21 reset-gated deployed scenarios, including focus, v1/v2 advancement, reload, latest-release execution, and ContextDB mutation; a final hard reset deleted 226 records and 2 packages and a follow-up inventory returned zero. General record/aggregate effects remain open. See [decisions 0079](../decisions/0079-browser-authoritative-sync-has-four-cost-modes.md) and [0080](../decisions/0080-focus-exact-entities-and-evolve-arraylogic-immutably.md). |
| Protected-asset storage and audit | Implemented and development-verified bounded Phase 5B lifecycle | `onevar-platform` + `aws` + `compute` | Browser-only encrypted spans, exact bindings, hardware-gated presentation, approval sync, recipient-key rotation, fail-closed local speech, zero-knowledge recovery, and forward recipient removal are implemented. Phase 5B adds natural-name-to-exact-ID recipient rosters, cross-device recovery of rosters and encrypted files, 1 MiB authenticated file chunks, one-response compiler-owned protected text-to-PDF creation, owner-local protected schedules, and monotonic ordinary-to-protected ArrayLogic. No protected filename, file bytes, ciphertext, key, protected workflow value, or generated PDF enters server transport. Portable packaged ASR, additional file transforms, automatic remaining-member rewrap, and autonomous owner-local work while every owner device is absent remain incomplete. See [decisions 0068](../decisions/0068-browser-only-protected-span-sealing.md), [0069](../decisions/0069-exact-owner-local-protected-context-and-presentation.md), [0073](../decisions/0073-hardware-gated-presentation-and-recipient-use.md), [0093](../decisions/0093-zero-knowledge-protected-recovery-envelopes.md), [0094](../decisions/0094-recipient-removal-rotates-current-ciphertext.md), [0098](../decisions/0098-shared-cost-reconciliation-and-local-protected-composition.md), and [0109](../decisions/0109-owner-local-files-schedules-and-monotonic-arraylogic.md). |
| Recipient-specific protected sharing | Implemented exact hardware-at-use boundary and natural-addressed independent group fan-out | `onevar-platform` + `aws` + `compute` | Exact recipient public keys, safe binding descriptors, durable requests, owner-local content-key rewrap, opaque server material, and recipient-only decryption are implemented. Natural profile membership resolves authorized ordinary Context to one exact entity ID. Group approval selects only pending requests from exact active member IDs and creates a separate wrap and grant for each; no group key exists. Group rosters recover through the separately stored zero-knowledge recovery envelope. Automatic rewrap for remaining members remains incomplete. See [decisions 0070](../decisions/0070-clean-recipient-specific-zero-trust-sharing.md), [0073](../decisions/0073-hardware-gated-presentation-and-recipient-use.md), [0093](../decisions/0093-zero-knowledge-protected-recovery-envelopes.md), [0094](../decisions/0094-recipient-removal-rotates-current-ciphertext.md), and [0109](../decisions/0109-owner-local-files-schedules-and-monotonic-arraylogic.md). |
| Owner-local protected Compute | Implemented bounded numeric, protected-file/PDF, schedule, and monotonic ArrayLogic slice | `onevar-platform` | Convert freezes one semantic model answer, then trusted code deterministically compiles the complete hash-pinned bounded numeric or protected text-to-PDF contract and JPL; no second model answer can enlarge protected authority. Exact WebAuthn authority is scoped to installation/capability/version/operation/requirements/bindings/program/purpose. ArrayLogic may run an ordinary prefix followed by an owner-local protected suffix; typed ordinary outputs may enter the worker, but protected values cannot return to ordinary/server steps. Due protected schedules wait for an authorized owner browser and commit only an exact value-free receipt. Development release `df4fd81` passed 22 reset-gated deployed browser scenarios and a real `gpt-5.4-mini` protected calculation authoring/execution canary in workflow [33258275841](https://github.com/jaustinhughesgit/onevar-platform/actions/runs/33258275841); the live job moved directly from `answer_plan_pending` to deterministic validation and publication. Additional file transforms, protected effects/provider calls, arbitrary JavaScript, cross-user protected service execution, and autonomous execution without an owner device remain incomplete. See [decisions 0071](../decisions/0071-minimized-owner-local-protected-compute.md), [0072](../decisions/0072-hardware-gated-owner-local-protected-compute.md), [0098](../decisions/0098-shared-cost-reconciliation-and-local-protected-composition.md), and [0109](../decisions/0109-owner-local-files-schedules-and-monotonic-arraylogic.md). |
| Trusted provider protected executor | Implemented and deployed through provider-aware selection | `onevar-platform` | Phase 4D treats app credentials as versioned Protected Assets rather than per-user Secrets Manager secrets. The browser explicitly promotes one exact local binding into trusted-server ciphertext and wraps its content key for one stage-scoped asymmetric KMS key. Exact server-verified WebAuthn grants bind the actor policy, asset/binding/executor key/capability/version/operation/requirement/program/adapter/host/disclosure and once/timed authority. The browser-facing API cannot decrypt; the isolated executor can decrypt only with its exact KMS key and has no DynamoDB or Secrets Manager access. The model selects one immutable reviewed-operation ID; trusted catalog code expands the complete contract and compiler-owned JPL. Catalog/model punctuation variants collapse to one runtime frame. Non-dependency slots carry typed ordinary local-Path values without inventing Context or `use` authority; dependency slots alone bind exact entity/relation IDs. Foundation release `4a373ddedb8651af076c0364b0792f14f9a35799` and lifecycle release `9632cf6b31323aa05af09c84623b0d5c5590b93a` prove no-network credential use and recovery. Phase 4D.2 release `bc0ff4861b143797e4db6f5412837f850a2e3356` adds an exact HTTPS/host/method/path/query/time/size/JSON network cell and one immutable WeatherAPI current-weather adapter that releases only a sanitized summary or safe diagnostic. Clean-reset development workflow `32971061118` passed the live-model/real-provider synthetic-key canary; production workflow `32971602722` promoted the same SHA and passed all deployed browser scenarios. Phase 4E.1 release `8670955f9d6040132a2057740816ed442764a822` adds catalog-owned provider ID/name/domain/kind/environment/scope/help provenance, publish-time credential requests, a provider-specific local selector, vague-label rejection, exact rotation, and revocation. Clean-reset development workflow `32975295573` passed the live provider-aware canary, and production workflow `32975839314` promoted the identical SHA. Exact IDs—not the displayed domain—remain binding and authorization identity. Encrypted protected outputs, write/paid/non-idempotent provider contracts, broader credential-selection UI, reservation/commit recovery, scheduling, and marketplace governance remain incomplete. See [decision 0075](../decisions/0075-protected-app-credentials-use-executor-wraps.md). |
| Protected provider result, effect, and test-credit lifecycle | Implemented and deployed from a clean global reset | `onevar-platform` | Phase 4E.2 encrypts trusted-ephemeral provider results inside the isolated executor to one exact active browser ECDH key, so the API stores only addressed ciphertext and value-free evidence. Phase 4E.3 adds exact invocation-id idempotency, pre-execution cancellation, durable recovery, definitive no-effect failure, and mandatory provider-effect proof for writes. Phase 4E.4 adds balanced non-cash test-credit accounts and atomic reservation/settlement/refund with separate provider cost, publisher earning, and platform revenue. `reset context` preserves durable credits, ledgers, operations, effects, and settlements; only the private hard global reset deletes them with the test entity. Release `bf1560c6d9cd7623f2ecc3960f5c354b2c045bd6`, workflow `32997983235`, and a 42.8-second clean live-model canary prove all four lifecycle fixtures. No-network appointment, paid-success, and paid-failure fixtures prove the reusable lifecycle without granting arbitrary network or payment authority. Real provider writes, Stripe purchase/payout, variable pricing, scheduling, and marketplace governance remain incomplete. See [decision 0076](../decisions/0076-protected-provider-results-effects-and-test-credit-settlement.md). |
| Durable browser notifications and email fallback | Implemented, deployed, and two-user verified in development / suppression transport acceptance pending | `onevar-platform` | Strict masked-contact and value-free notification contracts persist independently of acknowledgement. One-minute delayed SQS processing atomically checks the exact active contact and claims at most one fixed generic SES reminder per unresolved set; the isolated worker decrypts only after claim and final eligibility checks. Verification permits five sends per account per 24 hours and five incorrect guesses per code. RFC 8058 unsubscribe, owner preference, and idempotent direct/SNS delivery, bounce, and complaint feedback disable future sends. Owner verification, generic reminder, protected request, approval, and recipient authorization passed two-user development acceptance. Denial/reload, live unsubscribe/preference-off, SES simulator suppression, and SQS/DLQ acceptance remain. See [durable notifications](capabilities/durable-notifications.md), [decision 0115](../decisions/0115-durable-notifications-precede-generic-email.md), and [decision 0117](../decisions/0117-consent-governed-email-fallback-fails-closed.md). |
| Sunburst context projection and marketplace templates | Partial — development foundation deployed; fixed-slot redesign implemented in source | `onevar-platform` + canonical marketplace target | Automated tests cover strict template/overlay/attention contracts, trusted fixed rainbow slots with a permanent purple `system.1var` ray, the compact right-side Sunburst/four-button-slider transition, persistent wheel selection and input tint until another ray is selected, center-hold ordinary Essence capture, the canonical vector-logo center, upward widget dashboard, lossless browser-local topic-bank swaps, five baseline widget cards whose non-placeholder selections submit directly through the command runtime, immediate first-slot projection when Convert registers an app under the selected ray group, invocation-deduplicated successful-execution attention with 1–3/4–19/20+ count floors plus weighted urgency, deterministic multi-facet resolution, bounded authorized Context selection, immutable active-assignment materialization, and the signed S3/Dynamo marketplace route family. Opening/focus does not create attention. Color and placement remain presentation evidence, never identity or authority. Authenticated deployed proof of the redesigned control, durable cross-device bank/attention/widget feeds and source adapters, live category-aware ContextDB retrieval, employer/group distribution, complete marketplace lifecycle, and derived topic/facet Position integration remain incomplete. See [Sunburst context projection](capabilities/sunburst-context-projection.md), [decision 0118](../decisions/0118-sunbursts-are-marketplace-lenses-with-a-trusted-system-ray.md), [decision 0119](../decisions/0119-sunburst-slots-own-color-and-topics-swap-losslessly.md), and [decision 0120](../decisions/0120-sunburst-attention-is-event-derived-not-navigation-derived.md). |
| Organization-managed zero-trust keys | Product intent / foundation | `aws` + `compute` | Recipient key wrapping can support company-managed sharing; grant, rewrap, removal, rotation, and device lifecycle are incomplete. |
| Local protected-asset companion | Implemented bounded browser calculation / partial general companion | `onevar-platform` + future local runtime | Phase 4C proves browser-managed numeric JPL calculation against exact protected bindings without server plaintext. Broader local providers, files, native capabilities, protected ASR, recovery, and independently verifiable client delivery remain incomplete. |
| Three execution planes and `fileWorker` isolation | Implemented envelope foundation / partial hardening | `aws` + `compute` | Execution-envelope v1 now spans browser-main ownership, dynamic local `fileWorker`, and governed Compute/JPL without merging authority. `fileWorker` may request effects but cannot mark them applied; executable source is excluded from envelopes. Legacy calls, complete JPL effect declaration, same-origin network authority, resource policy, output sanitization, CSP, and hardened isolation remain work. See [execution envelopes](execution-envelopes-and-governed-interaction.md), [worker isolation](capabilities/worker-isolation.md), and [decision 0029](../decisions/0029-versioned-execution-intent-and-interaction-effects.md). |
| Sound module | Implemented foundation | `aws` | Web Audio lifecycle, tone/media playback, stop, analyzer, and bus integration exist. |
| Reusable API/provider protocols | Implemented compiler foundation / emerging catalog | Entities + Paths + JPL | Ordered declarative request chaining, protected credential injection, official-domain repair research, bounded HTTP, and typed output validation exist. Durable published protocol lineages and broad provider catalog reuse remain incomplete. |
| Headless website protocol capture | Product intent | Entities, moods, Paths, local runtime | Model-assisted builder with approval; resulting protocol should be reusable and versioned. |
| Mindsets, thoughts, moods | Implemented first-class primitives / partial spec | Entity/front-end modules | Not architectural legacy. Runtime representation exists; publication, composition, and authoring semantics need dedicated documentation. |
| Provider-neutral conferencing | Implemented and live-proven in development | `onevar-platform` + Amazon Chime SDK Meetings | Exact session/invitation/participant-grant contracts, a persistent browser call surface, name-to-exact invitee resolution, invitation sync, distinct response-only attendee tokens, value-free joined-presence evidence, third-user denial, organizer-only end, and provider cleanup are implemented. Compute/ArrayLogic compose the primitive rather than owning media transport. Development release `47e0d4f40767119b7561e5c575515b9589cb9754` and workflow [33596790583](https://github.com/jaustinhughesgit/onevar-platform/actions/runs/33596790583) live-prove two exact browsers joining one Chime meeting, remote-presence projection, call-surface persistence through a UI change, outsider denial, and organizer termination. Recording/caption/protected-media policy, multi-party rooms, billing, load, and production proof remain incomplete. See [real-time conferencing](capabilities/realtime-streaming.md) and [decision 0127](../decisions/0127-conferencing-is-a-core-session-primitive-composed-by-compute.md). |
| Legacy Kinesis Streaming/WebRTC | Legacy POC reference | `aws` + `compute` + Kinesis Video Streams | Presence, invites, media capture, master/viewer signaling, and scoped credentials remain useful prior-art, but are not the clean platform's current authority model. |
| Email entity addresses | Partial | `aws` + `compute` + SES | Entity-addressed outbound behavior exists; inbound ownership/routing contract needs verification and specification. |
| Email consent/reputation controls | Partial | `compute` + SES | Opt-in, unsubscribe, rate, block, bounce, and suppression controls exist; complaint handling and deployment configuration verification remain gaps. |
| Position/Search/RAG derived retrieval | Implemented foundation / partial scale contract | Cross-layer | New anchor postings put the shard in the partition key and carry canonical revision/hash. Compute manifests now publish sanitized semantic capability Positions with exact contract hashes; Convert queries bounded Search candidates instead of listing the catalog when indexed Search is available. A reusable Convert build is acknowledged only after bounded Position projection succeeds; canonical persistence without its derived posting remains an explicit retryable build failure. Position derives owner/policy from authenticated server records. Search unions v2 shards and legacy v1, reloads current subdomains, distinguishes read visibility from explicit `use`, authorizes, and then ranks. Existing-capability backfill, stale removal, exact reranking, browser payload convergence, and deployed large-scale cost/load tests remain incomplete. |

| Exact Compute entity-use reconciliation | Implemented foundation | `compute` + `aws` + `testing` | After semantic operation selection, one browser-resolved invocation target plus a declared current/result transition can derive one exact supplied relation binding without model ID copying. A qualified possessive retains the owner identity separately and binds the one exact owned target reached through its ownership edge. A reset-gated deployed browser proof installed a new caller Path with the exact dependency, target entity, relation, subject, publisher, version, and named-hydration provenance. Ambiguity still requires an exact validated model proposal or fails closed. See [decision 0048](../decisions/0048-compute-installations-bind-entity-use-by-exact-id.md). |
| Owner-published delegated Context effects | Implemented foundation | `compute` + `aws` + `testing` | A caller with exact `use` authority may run a user-owned capability against that capability owner's ordinary public relation. Question and imperative possessive syntax preserve the owner/object edge through discovery; a qualified writable Path cannot install without one exact relation-ID binding per dependency. One validated fixed replacement effect with one unambiguous literal result compiles deterministically after semantic discovery and returns that same result directly at the governed entity boundary instead of requiring a redundant model-authored or legacy-loaded implementation. The service endpoint verifies app/version/operation/dependency, app owner equals relation publisher, exact target IDs/version/current value, audience visibility, and protected-data exclusion before versioning only the manifest-declared transition. The browser retains the declared scalar label under the acknowledged canonical value ID, proves the local rewire, and excludes it from caller publication. The reset-gated deployed two-browser proof changed exactly one Austin-owned relation and independently read the clean value from User 2. Third-party-app delegation, organizations, protected data, and broader edit policies remain future versioned contracts. See [decisions 0046](../decisions/0046-browser-applied-compute-context-effects.md) and [0051](../decisions/0051-owner-published-compute-delegates-bounded-context-effects.md). |
| Compute command-pattern Position projections | Implemented foundation / deployed proof pending | `compute` + `testing` | Signature bundle v3 indexes the sanitized full contract plus independently embedded generalized invocation patterns rendered from declared input names, such as `wash my vehicle`. Raw example values and protected values are excluded; canonical manifest reload and explicit `use` authority remain mandatory. Existing definitions require backfill, and live short-command reuse proof is pending. See [decision 0039](../decisions/0039-compute-contract-fingerprints-and-positioning.md). |

Pricing clarification (2026-08-28): [decision 0105](../decisions/0105-conservative-application-cost-estimates-and-one-spoken-root.md) supersedes the older requirement that AWS Cost Explorer reconciliation gate ordinary charging. One spoken interaction now has one root across transcription and its downstream work. Versioned application estimates cover the admitted Lambda, DynamoDB, S3, CloudFront/API transfer, and related request footprint immediately; tagged Cost Explorer data is a later diagnostic for calibrating those conservative estimates, not a prerequisite for charging. Exact external payment and provider money events still require their authoritative provider evidence.

| Signed advanced execution and real provider writes | Implemented Phase 5C runtime boundary; deployed promotion pending | `onevar-platform` | Immutable adapter releases bind independent review, signature, exact package/version/digest, execution class, broker authority, and allowed hosts. Fresh workers have no ambient file/network access; native/large/CPU packages require an explicit container executor. Unknown, revoked, expired, or mismatched releases fail closed without dynamic npm installation. The first real reviewed write creates one Todoist task with a user-owned protected token, fixed host/path, bounded JSON, invocation-scoped `X-Request-Id`, and committed provider-effect evidence. Broader package/provider admission and non-idempotent writes remain review work. See [decision 0110](../decisions/0110-advanced-execution-is-signed-exact-and-explicitly-reversible.md). |

## Adding a capability

When a capability is found or proposed, record:

1. User and builder value
2. Owning primitives and layers
3. Current evidence in code or tests
4. Trust and authorization boundary
5. Lifecycle and versioning
6. Known gaps
7. Whether it supersedes or generalizes another entry
